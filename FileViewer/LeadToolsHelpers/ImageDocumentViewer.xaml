<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FileViewer.LeadToolsHelpers">
    <Style TargetType="{x:Type local:ImageDocumentViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageDocumentViewer}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid
                            x:Name="ThumbnailPanel">
                            <TextBlock Text="Hi"></TextBlock></Grid>
                        <Grid
                            x:Name="BookmarkPanel">
                            <TextBlock
                                Text="Hi"></TextBlock>
                        </Grid>
                        <Grid
                            x:Name="ViewerPanel"></Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>